export enum InviteStatusEnum {
    SENT = 'sent',
    PENDING = 'pending',
    PROCESSING = 'processing',
    FAILED = 'failed',
}

export enum InterviewTypeEnum {
    SHARED = 'shared',
    OWNED = 'owned',
}
